import { DropFileZoneDirective } from './drop-file-zone.directive';

describe('DropFileZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DropFileZoneDirective();
    expect(directive).toBeTruthy();
  });
});
